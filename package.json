{
  "name": "codex-token-meter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:mock": "vite --mode mock",
    "build": "tsc --noEmit && vite build",
    "test": "bun test",
    "tauri": "tauri",
    "tauri:mock": "CODEX_MOCK_USAGE=true tauri dev"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "@types/bun": "^1.2.0",
    "@tauri-apps/cli": "^2.5.0"
  }
}
